# export PATH=$HOME/bin:$HOME/.local/bin:/usr/local/bin:$PATH       # If you come from bash you might have to change your $PATH.




# Terminal Customisation
# [[ -f $HOME/.zsh/zsh.zsh ]] && source $HOME/.zsh/zsh.zsh

# >>> ansible >>>
# eval $(register-python-argcomplete ansible)
# eval $(register-python-argcomplete ansible-config)
# eval $(register-python-argcomplete ansible-console)
# eval $(register-python-argcomplete ansible-doc)
# eval $(register-python-argcomplete ansible-galaxy)
# eval $(register-python-argcomplete ansible-inventory)
# eval $(register-python-argcomplete ansible-playbook)
# eval $(register-python-argcomplete ansible-pull)
# eval $(register-python-argcomplete ansible-vault)

# alias keylogserver="docker run -d -p 8080:8080 --name keylogger-app keylogger-server"
# alias flagfolder="python3 $HOME/.hacking/folder.py"

# export PATH=/usr/local/zeek/bin:$PATH

# To customize prompt, run `p10k configure` or edit ~/dotfiles/stow/zsh/.p10k.zsh.
# [[ ! -f ~/dotfiles/stow/zsh/.p10k.zsh ]] || source ~/dotfiles/stow/zsh/.p10k.zsh
# rclone-aliases